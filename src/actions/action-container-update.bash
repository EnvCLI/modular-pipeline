#!/usr/bin/env bash
set -euo pipefail

# Main Function
#
# Returns the exit code of the last command executed or 0 otherwise.
function main()
{
  # will check if there are new updates available and update the repo if needed to trigger a rebuild
  echo "Doing update ..."
}

# entrypoint
main "$@"
